# 自然物种查询门户 - 快速启动指南

## 🚀 方法一：快速启动（推荐）

### 1. 进入前端目录
```bash
cd /mnt/public7/pancancercol/hefeng/inaturalist/frontend
```

### 2. 安装依赖
```bash
pip install flask flask-cors
```

### 3. 启动应用
```bash
python app.py
```

### 4. 访问网站
打开浏览器访问：**http://localhost:5000**

---

## 🔧 方法二：使用虚拟环境（推荐开发使用）

### 1. 创建虚拟环境
```bash
cd /mnt/public7/pancancercol/hefeng/inaturalist
python3 -m venv venv
```

### 2. 激活虚拟环境
```bash
# Linux/Mac
source venv/bin/activate

# Windows
venv\Scripts\activate
```

### 3. 安装依赖
```bash
pip install flask flask-cors requests
```

### 4. 启动前端
```bash
cd frontend
python app.py
```

---

## 📦 方法三：使用项目 requirements.txt

### 1. 安装所有依赖
```bash
cd /mnt/public7/pancancercol/hefeng/inaturalist
pip install -r requirements.txt
pip install flask flask-cors
```

### 2. 启动前端
```bash
cd frontend
python app.py
```

---

## 🌐 访问方式

启动成功后，你可以通过以下方式访问：

| 地址 | 说明 |
|------|------|
| http://localhost:5000 | 本机访问 |
| http://127.0.0.1:5000 | 本机访问（IP方式）|
| http://你的IP:5000 | 局域网其他设备访问 |

---

## 📱 页面功能介绍

启动后你可以访问以下页面：

### 1. 首页 (http://localhost:5000/)
- 搜索入口
- 热门物种快捷链接
- 最新研究级观察记录

### 2. 搜索页 (http://localhost:5000/search)
- 物种搜索（支持自动补全）
- 分类等级筛选
- 结果分页

### 3. 物种详情页 (http://localhost:5000/species/8318)
- 物种基本信息
- 图片画廊
- 分类树
- 观察记录
- Wikipedia 摘要

### 4. 观察记录页 (http://localhost:5000/observations)
- 研究级观察记录列表
- 按物种筛选
- 按位置筛选

### 5. 地图页 (http://localhost:5000/map)
- 交互式地图
- 点击任意位置查看周围物种
- 显示物种分布

---

## 🔌 API 接口测试

启动后，你可以直接测试 API 接口：

```bash
# 测试搜索接口
curl "http://localhost:5000/api/search?q=喜鹊&per_page=5"

# 测试物种详情
curl "http://localhost:5000/api/species/8318"

# 测试自动补全
curl "http://localhost:5000/api/autocomplete?q=ma&per_page=10"

# 测试观察记录
curl "http://localhost:5000/api/observations?taxon_id=8318&per_page=5"
```

---

## 🛠️ 常见问题

### 1. 端口被占用
如果 5000 端口被占用，修改 `app.py` 最后一行：
```python
app.run(debug=True, host="0.0.0.0", port=8080)  # 改为 8080 或其他端口
```

### 2. 外部网络访问
如果需要允许外部访问，确保：
1. 使用 `host="0.0.0.0"`（已在代码中设置）
2. 防火墙开放对应端口
3. 云服务器需要在安全组中开放端口

### 3. 依赖安装失败
```bash
# 升级 pip
pip install --upgrade pip

# 单独安装
pip install flask
pip install flask-cors
```

---

## 📝 开发模式

如需开发调试模式（代码修改自动重启）：

```bash
export FLASK_APP=app.py
export FLASK_ENV=development
flask run --host=0.0.0.0 --port=5000
```

---

## 🎉 启动成功标志

看到以下输出表示启动成功：

```
============================================================
🌿 iNaturalist 自然物种查询门户
============================================================
访问地址: http://localhost:5000
============================================================
 * Running on http://0.0.0.0:5000
```

现在打开浏览器访问 **http://localhost:5000** 即可使用！
