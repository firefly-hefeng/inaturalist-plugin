# 自然物种查询门户 - 前端

基于 iNaturalist 插件的独立前端应用，使用 Flask + Bootstrap 构建。

## 功能特性

- 🔍 **物种搜索** - 支持中文、英文、学名搜索，自动补全建议
- 📸 **物种详情** - 展示分类信息、图片、Wikipedia 摘要、保护状态
- 🗺️ **地图浏览** - 在地图上探索物种分布，查看周围物种种类
- 📷 **观察记录** - 浏览研究级观察记录，按位置筛选
- 📱 **响应式设计** - 适配桌面和移动设备

## 安装运行

### 1. 安装依赖

```bash
cd frontend
pip install flask flask-cors
```

### 2. 运行应用

```bash
python app.py
```

### 3. 访问

打开浏览器访问: http://localhost:5000

## 项目结构

```
frontend/
├── app.py                 # Flask 后端
├── README.md             # 本文件
├── templates/            # HTML 模板
│   ├── base.html         # 基础模板
│   ├── index.html        # 首页
│   ├── search.html       # 搜索页面
│   ├── species_detail.html  # 物种详情
│   ├── observations.html    # 观察记录
│   └── map.html          # 地图页面
└── static/               # 静态文件
    ├── css/
    │   └── style.css     # 样式表
    └── js/
        └── main.js       # 主脚本
```

## API 接口

前端通过以下 API 接口与后端通信：

| 接口 | 说明 |
|------|------|
| `GET /api/search` | 搜索物种 |
| `GET /api/species/<id>` | 获取物种详情 |
| `GET /api/species/<id>/images` | 获取物种图片 |
| `GET /api/autocomplete` | 自动补全建议 |
| `GET /api/observations` | 搜索观察记录 |
| `GET /api/location/species` | 获取位置周围物种 |
| `GET /api/taxonomy/<id>/children` | 获取子分类群 |
| `GET /api/taxonomy/<id>/ancestors` | 获取祖先分类群 |

## 配置说明

可在 `app.py` 中修改以下配置：

```python
# 修改端口
app.run(debug=True, host="0.0.0.0", port=5000)
```

## 截图预览

- 首页: 搜索入口、热门物种、最新观察
- 搜索页: 物种列表、筛选、分页
- 详情页: 物种信息、图片、分类树
- 地图页: 交互式地图、位置搜索

## 技术栈

- **后端**: Python Flask
- **前端**: HTML5, CSS3, JavaScript
- **UI 框架**: Bootstrap 5
- **地图**: Leaflet.js + OpenStreetMap
- **图标**: Font Awesome

## 许可

MIT License
